import 'dart:ui';

class AppColors {
  // absolute colors
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const white60 = Color(0x9AFFFFFF);

  // identity colors
  static const primary = Color(0xFF0080FF);
  static const accent = Color(0xFFFFD500);

  // status colors
  static const attention = Color(0xFFE54545);
  static const success = Color(0xFF57D982);

  // text hieracy colors
  static const text = Color(0xFF0A121A);
  static const secondary = Color.fromARGB(154, 51, 59, 67);

  // widget hieracy colors
  static const surface = Color(0xFFF0F1F2);
  static const border = Color(0x1A525C66);
  static const tinted = Color(0xCCEFF2F9);
}
